server:
  port: 8502
spring:
  cloud:
    consul:
      discovery:
        health-check-path: /home
        health-check-interval: 10s
        instance-id: consul-server01
        service-name: consul-server
      host: localhost
      port: 8500

  application:
    name: consul-server
